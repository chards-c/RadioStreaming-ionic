import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.josecedeno.app',
  appName: 'appRadio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
